"""
The ``text_indentation`` function
================================

This is a function that prints a text with 2 new lines after '.', '?', and ':'.

Using ``text_indentation``
---------------------------

Import the ``text_indentation`` function

>>> text_indentation = __import__('5-text_indentation').text_indentation

# Test 1: Print text with two new lines after '.'
>>> text_indentation("Hello. This is a test.")
Hello.
<BLANKLINE>
This is a test.
<BLANKLINE>

# Test 2: Print text with two new lines after '?'
>>> text_indentation("What is this?")
What is this?
<BLANKLINE>

# Test 3: Print text with two new lines after ':'
>>> text_indentation("Here is the result:")
Here is the result:
<BLANKLINE>

# Test 4: Print text with two new lines after multiple '.', '?', and ':'
>>> text_indentation("This. Is? A: Test.")
This.
<BLANKLINE>
Is?
<BLANKLINE>
A:
<BLANKLINE>
Test.
<BLANKLINE>

# Test 5: Text is None
>>> text_indentation(None)
Traceback (most recent call last):
    ...
TypeError: text must be a string

# Test 6: Empty text
>>> text_indentation("")
<BLANKLINE>

# Test 7: Text with no '.', '?', or ':'
>>> text_indentation("No punctuation here")
No punctuation here
<BLANKLINE>

# Test 8: Text with multiple new lines
>>> text_indentation("Multiple\nnew\nlines.")
Multiple
new
lines.
<BLANKLINE>

# Test 9: Text with leading and trailing spaces
>>> text_indentation("   Spaces   everywhere.   ")
Spaces   everywhere.
<BLANKLINE>

# Test 10: Text with numbers
>>> text_indentation("Numbers: 1, 2, 3. What?")
Numbers: 1, 2, 3.
<BLANKLINE>
What?
<BLANKLINE>
"""
